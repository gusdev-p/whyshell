#ifndef INPUT_H
#define INPUT_H
#include "./config.h"



/**
 * ## setInput
 * @brief Get the base input and parse to a final input
 * @param config The config to get the input and define the final input.
 */
int setInput(Config *config);

#endif