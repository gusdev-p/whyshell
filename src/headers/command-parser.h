#ifndef COMMAND_PARSER_H
#define COMMAND_PARSER_H

/**
 * ## parseCommand
 * @brief parse all whyshell built-in commands and execute
 * @param input The input to check the commands
 */
void parseCommand(char *input);

#endif